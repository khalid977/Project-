#include <stdio.h>
#define cycle 60
#idef_WIN 32 
#include<conio.h>
define sleep(x) Sleep(x*1000)
#else
#define clrscr() printf("\e[i;H\e[2j")
#include <inistd.h)
int main (){
    int hour,minute,seconds;
    printf("Enter Hour Minute Second :");
    scanf(%d%d%d,&hour,&Minute&second);
    clrser();
    int h=0,m=0,s=0;
    while(1)
    printf("\n\n### Stopwatch###")
printf("%.2d : %.2d : %.2d : \n,h ,m ,s );
Printf(\n\n\n\n#####################\n\n\n)
if (h==houreff&&m==minut&&s== Second
break;
}else
{clrscr();
}
s++;
sleep(1);
if (s == cycle){
    m++
    s=0;
    
}

return 0;
}
